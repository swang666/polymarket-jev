# Copy to .env  (already gitignored -- never commit the real one)
#
# Get a key at https://console.typesafe.ai/
TYPESAFE_API_KEY=sk-your-key-here

# Optional: pin a model version instead of jev-latest
# TYPESAFE_MODEL=jev-1.13.0
